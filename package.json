{
  "name": "advice.js",
  "version": "0.0.0",
  "description": "Simple AOP module",
  "scripts": {
    "prepublish": "npm run build",
    "test": "npm run build && karma start",
    "build": "browserify -s advice index.js > advice.js"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/dwango-js/advice.js.git"
  },
  "devDependencies": {
    "browserify": "~9.0.3",
    "karma": "~0.12.31",
    "karma-cli": "0.0.4",
    "karma-jasmine": "~0.3.5",
    "karma-phantomjs-launcher": "~0.1.4"
  }
}
